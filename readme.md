# MARK XXXIX

A Python-based AI assistant interface by MAL19INDUSTRIES.

## Setup

1. Install Python dependencies:
   ```bash
   python -m pip install -r requirements.txt
   ```
2. Install Playwright browsers:
   ```bash
   python -m playwright install
   ```

## Run

```bash
python main.py
```

If your system uses `python3`, use `python3 main.py` instead.

## Notes

- Ensure `config/api_keys.json` contains your Gemini API key under `gemini_api_key`.
- Credits to: FaithMakes for Inspiring!
